- [x] Update `compress.py` to use `gs` for real PDF compression
- [x] Add tests for `compress_pdf` with an image-heavy PDF
- [x] Ensure `compress_pdf` signature remains identical (including `on_progress`)
